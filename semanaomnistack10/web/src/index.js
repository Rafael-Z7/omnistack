import React from 'react'; //importa o react, tudo que usa html
import ReactDOM from 'react-dom';

import App from './App';


ReactDOM.render(<App />, document.getElementById('root')); // renderiza por parametros app no elemento capturadod por id

